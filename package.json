{
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "@tailwindcss/typography": "^0.5.20",
    "tailwindcss": "^4.3.3"
  },
  "scripts": {
    "update": "npx npm-check-updates -u && npm install",
    "dev": "rm -rf public && hugo --gc && npx pagefind --site public && hugo server"
  },
  "allowScripts": {
    "@parcel/watcher@2.5.1": true
  }
}
