{
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.18",
    "@tailwindcss/typography": "^0.5.19",
    "tailwindcss": "^4.1.18"
  },
  "scripts": {
		"update": "npx npm-check-updates -u && npm install",
		"rebase": "git pull --rebase --autostash origin main",
		"dev": "hugo --gc && hugo server"
	}
}
